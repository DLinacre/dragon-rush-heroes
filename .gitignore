# Runtime data (snapshots + write-ahead log)
.data/
.data-*/

# Test artefacts
.shots/

# Dependencies (there are none at runtime, but tooling may add some)
node_modules/

# Environment
.env
.env.*

# Static demo build output (regenerated by CI for Pages)
demo/
